pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo "APK URL = ${params.APK_DOWNLOAD_PATH}"
                echo "APK URL = ${params.APK_DEPLOY_PATH}   "
                curl -O   ${APK_DOWNLOAD_PATH} 
                
            }
        }
    }
}