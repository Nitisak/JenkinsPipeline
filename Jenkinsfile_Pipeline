pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo 'APK URL = ${APK_DOWNLOAD_PATH}' 
                echo 'APK URL = ${APK_DEPLOY_PATH}' 
                
            }
        }
    }
}